[package]
name = "lazyusf2-rs"
version = "0.1.0"
edition = "2021"
description = "Rust wrapper for lazyusf2 (N64 sound format decoder using Mupen64Plus)"

[dependencies]
libc = "0.2"

[dev-dependencies]
rodio = "0.22"

[build-dependencies]
cc = "1"
