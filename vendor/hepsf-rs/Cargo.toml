[package]
name = "hepsf-rs"
version = "0.1.0"
edition = "2021"
description = "Rust wrapper for sexypsf (PS1 via sexypsf) and Highly Experimental (PS2 via HE) sound format decoders"

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1"
