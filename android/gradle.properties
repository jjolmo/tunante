org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
# The Rust cdylib and the decoder are staged into app/src/main/jniLibs by
# build.sh, not produced by Gradle, so there is nothing for AGP to strip and
# nothing it knows how to rebuild.
android.native.buildOutput=verbose
