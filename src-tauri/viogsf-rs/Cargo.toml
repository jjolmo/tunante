[package]
name = "viogsf-rs"
version = "0.1.0"
edition = "2021"
description = "Rust bindings for viogsf (VBA-M based GBA/GSF audio decoder)"
license = "GPL-2.0"

[dependencies]
libc = "0.2"

[build-dependencies]
cmake = "0.1"
